# Cold Start – Code Runner Backend

A minimal Programiz-style code execution backend designed to be used
with Godot or web-based editors.

## Run
```bash
pip install -r requirements.txt
uvicorn main:app --reload
